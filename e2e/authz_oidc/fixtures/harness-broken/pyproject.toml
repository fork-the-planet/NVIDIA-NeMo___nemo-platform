[project]
name = "harness-broken"
version = "0.0.1"
description = "E2E authz fixture plugin: import-time failure (unenumerable plugin -> namespace fence)."
requires-python = ">=3.11"
dependencies = []

[project.entry-points."nemo.services"]
harness-broken = "harness_broken.service:BrokenService"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/harness_broken"]
