Most benchmarks require a Hugging Face token (`hf_token`) to access gated datasets. Create this secret before running evaluations:

```python
import os

client.secrets.create(
    workspace=workspace,
    name="hf_token",
    value=os.getenv("HF_TOKEN", "<your Hugging Face token>"),
)
```
