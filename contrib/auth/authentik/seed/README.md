# Authentik Seed Notes

Populate this directory with the provider export or bootstrap material needed to
create the demo user, demo machine identity, groups, and OAuth client described
in `manifest.yaml`.
